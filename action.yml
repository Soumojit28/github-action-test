name: Github Action Test
author: Soumojit Ash

inputs:
  MESSAGE:
    description: 'Message to be printed'
    required: true
    default: 'Hello, World!'

runs:
  using: 'docker'
  image: 'docker://soumojit28/github-action-test:latest'

branding:
  icon: 'activity'